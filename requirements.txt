# Everything the toolkit needs. Claude Code is the model, so there is no
# provider library here and no API key to get.
#
#   pip install -e .          installs these and puts job_hunter on the path
#   playwright install chromium
pydantic>=2.7
pyyaml>=6.0
jinja2>=3.1
playwright>=1.47
pypdf>=5.0
python-docx>=1.1
